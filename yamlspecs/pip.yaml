!include common.yaml
---
- package: pip for python 
  name: pip 
  version: "{{ pyversion.pip }}"
  vendor_source: https://github.com/pypa/pip/archive/{{ version }}.{{ extension }}
  description: >
    Pip support for python {{ python_version }}
