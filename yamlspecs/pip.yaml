!include common.yaml
---
- package: pip for python 
  name: pip 
  version: "{{ pkg_defaults.pyversionadd.pip }}"
  vendor_source: https://github.com/pypa/pip/archive/{{ version }}.{{ extension }}
  description: >
    pip support for python {{ pyversion }}.
