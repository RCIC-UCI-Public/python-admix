!include common.yaml
---
- package: gast
  name: gast
  version: "{{versions.gast}}"
  vendor_source: https://github.com/serge-sans-paille/gast/archive/refs/tags/{{version}}.{{extension}}
  description: |
    Gast {{version}} is a generic AST to represent Python2 and Python3’s
    Abstract Syntax Tree(AST). GAST provides a compatibility layer
    between the AST of various Python versions, as produced by 
    ast.parse from the standard ast module
