!include common.yaml
---
- package: mako
  name: Mako
  version: "{{versions.mako}}"
  vendor_source: https://files.pythonhosted.org/packages/{{versions.mako_hash}}/{{name}}-{{version}}.{{extension}}
  description: |
    Mako {{version}} is a super-fast templating language that borrows
    the best ideas from the existing templating languages
