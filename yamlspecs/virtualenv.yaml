!include common.yaml
---
- package: Virtual Python Environment builder
  name: virtualenv 
  version: "{{ pkg_defaults.pyversion.virtualenv }}"
  vendor_source: https://github.com/pypa/virtualenv/archive/{{ version }}.{{ extension }}
  description: >
    Virtualenv for python {{ pyversion }} is a tool for creating isolated ‘virtual’ python environments.
