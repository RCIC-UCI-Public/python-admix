!include common.yaml
---
- package: ipython_genutils
  name: ipython_genutils
  version: "{{versions.ipython_genutils}}"
  vendor_source: https://files.pythonhosted.org/packages/{{versions.ipython_genutils_hash}}/{{name}}-{{version}}.{{extension}}
  description: |
    This is obsoleted yet is still refered to by traitlets which is called by
    IPython. all 3 are from the same developers team.
    Verbatim project description on pypi.org is
    Pretend this doesn't exist. Nobody should use it.
