!include common.yaml
---
- package: SQLAlchemy for python 
  name: sqlalchemy 
  version: "{{versions.sqlalchemy}}"
  version_rel: "{{versions.sqlalchemy_rel}}"
  src_dir: "{{name}}-rel_{{version_rel}}"
  vendor_source: https://github.com/sqlalchemy/sqlalchemy/archive/rel_{{version_rel}}.{{extension}}
  description: >
    SQLAlchemy {{version}} for python {{python_version}} provides Database Abstraction Library
