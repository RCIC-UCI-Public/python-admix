!include common.yaml
---
- package: asttokens
  name: asttokens
  version: "{{versions.asttokens}}"
  vendor_source: https://files.pythonhosted.org/packages/{{versions.asttokens_hash}}/{{name}}-{{version}}.{{extension}}
  description: |
    The asttokens {{version}} annotates Python abstract syntax trees (ASTs) with
    the positions of tokens and text in the source code that generated them
